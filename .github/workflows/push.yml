name: Push

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    uses: ./.github/workflows/test.yaml

  scan:
    uses: ./.github/workflows/scan.yml